{
  "private": true,
  "scripts": {
    "install:all": "cd server && npm install --include=dev && cd ../client && npm install --include=dev",
    "build:apps": "npm run build --prefix server && npm run build --prefix client",
    "build": "npm run install:all && npm run build:apps",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client -- --host 127.0.0.1"
  },
  "dependencies": {
    "@clerk/clerk-react": "^5.61.5",
    "@clerk/express": "^2.1.5"
  }
}
